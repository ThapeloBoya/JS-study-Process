function stringSize() {
    let str1 = "str";
    let len = str1.length;

    if (len <= 3) {

        document.write(str1.toUpperCase())

    } else {
        document.write(str1);
    }
}

stringSize()
