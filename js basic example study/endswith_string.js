//checks if a string ends with java
function newString() {
    let str = "i am javad";

    if (str.endsWith("java")) {
        document.write(true);
    }
    else {
        document.write(false);
    }
}

newString()